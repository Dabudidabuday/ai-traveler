{
  "name": "adventure-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=client --if-present && npm run build --workspace=server --if-present",
    "start": "node server/dist/index.js",
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
